## 📅 Day 1 – C# Console Application Basics

### Learning Points
- Understanding the structure of a C# console application
- Using `namespace`, `class`, and the `Main` method
- Writing output to the console using `Console.WriteLine`
- Program execution flow starting from `Main`

---

## 📅 Day 2 – Operators & Control Flow

### Learning Points
- Using arithmetic operators (`+`, `%`)
- Applying comparison operators (`<`)
- Implementing logical operators (`&&`, `||`)
- Performing bitwise operations (`&`, `|`, `^`)
- Writing conditional logic using `if`, `else if`, and nested conditions
- Using `switch-case` for branching logic
- Character comparison for vowel detection
- Leap year calculation logic
- Manual date progression logic without using `DateTime`
- Mapping numeric month values to string representations

### Assignment

| Assignment Name | Description |
|----------------|------------|
| Operator & Date Logic Assignment | Practice operators, control flow, vowel detection, leap year logic, and manual calculation of the next calendar date |

---

## 📅 Day 3 – Arrays & Sorting

### Learning Points
- Declaring and initializing string arrays
- Using array length for loop control
- Implementing nested `for` loops
- Comparing characters within strings
- Swapping array elements manually
- Creating a basic alphabetical sorting algorithm
- Iterating through arrays using `foreach`

### Assignment

| Assignment Name | Description |
|----------------|------------|
| Array Sorting Assignment | Sort a list of ASEAN country names alphabetically without using built-in sorting methods |

---

## 📅 Day 4 – Methods & Object-Oriented Basics

### Learning Points
- Writing methods with return values
- Using constants to avoid hard-coded values
- Creating and calling local methods
- Defining classes and creating objects
- Differentiating between fields and properties
- Using access modifiers implicitly
- Grouping related data using classes
- Calling instance methods from `Main`

---
